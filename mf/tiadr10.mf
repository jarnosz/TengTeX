% Computer Modern Tengwar 10pt
if unknown cmbase: input cmbase fi

design_size := 10;
font_identifier:="tIAD"; font_size design_size*pt#;

input c-cmr;   % compute size parameters for CMR family

generate tiad      % switch to the driver file

